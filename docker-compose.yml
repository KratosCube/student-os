version: '3.8'

services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.next
    environment:
      # 👇 ZMĚNA 1: Absolutní cesta (jistota pro Linux)
      - DATABASE_URL=file:/app/prisma/dev.db
      - WATCHPACK_POLLING=true
    # 👇 ZMĚNA 2: Tento příkaz vytvoří DB (pokud není) a pak spustí web
    command: sh -c "npx prisma db push && npm run dev"